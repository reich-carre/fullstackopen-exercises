{
  "name": "xlesson3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../lesson2 && npm run build && cp -r dist ../backendL3 ",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push origin new-repo-alias"
  },
  "keywords": [],
  "author": "Reich",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "nodemon": "^3.1.11"
  }
}
